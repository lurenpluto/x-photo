# X-Photo 问题与修复跟踪

本文用于持续跟踪项目中的已知问题、修复计划、验证结果和下一步工作。新增问题先登记为 `TODO`；开始处理后改为 `IN_PROGRESS`；代码修复完成后改为 `FIXED`；对应验证通过后改为 `VERIFIED`。

## 状态说明

| 状态 | 含义 |
| --- | --- |
| `TODO` | 已确认或高度疑似，需要修复或进一步验证。 |
| `IN_PROGRESS` | 正在修复或验证中。 |
| `FIXED` | 已完成代码或文档修复，但还需要补充验证结果。 |
| `VERIFIED` | 已通过对应验证，可以视为关闭。 |
| `WONTFIX` | 明确暂不修复，需要在备注中说明原因。 |

## 使用规则

- 每个问题必须有稳定编号，格式为 `XP-YYYYMMDD-NN`。
- 新发现的问题追加到“问题列表”，默认状态为 `TODO`。
- 修复时在“修复计划/处理记录”中写清楚变更范围。
- 验证后在“验证结果”中记录命令、结论和失败摘要。
- 文档、后端、前端、脚本、测试问题都放在同一张表中，便于判断下一步优先级。

## 问题列表

| ID | 状态 | 优先级 | 模块 | 问题 | 影响 | 下一步 |
| --- | --- | --- | --- | --- | --- | --- |
| XP-20260425-01 | `VERIFIED` | P0 | service / scan | 扫描取消存在竞态，`cancel_scan_should_reach_cancelled` 实测失败，取消后终态可能变成 `success`。 | 用户点击取消后无法可靠停止任务，任务状态页会误导用户。 | 已修复 running scan 取消状态机，并通过针对测试与完整 `cargo test`。 |
| XP-20260425-02 | `VERIFIED` | P0 | service / scan | 成功扫描后仍持久化 `last_scanned_path`，后续手动扫描默认从游标之后开始。 | 新文件、旧文件变更、删除同步等场景可能漏扫。 | 已将恢复游标限定到 retry，成功扫描清空 source 级游标，并增加手动重扫回归测试。 |
| XP-20260425-03 | `VERIFIED` | P1 | service / daemon | 默认启用 source 变更检测，周期同步递归遍历整个 source。 | 大目录下可能长期占用 runtime 和磁盘 IO；fingerprint 也无法精确表达变化。 | 已改为默认关闭；显式开启时目录遍历放入 blocking 线程，并记录 fingerprint 耗时和 entry 数。 |
| XP-20260425-04 | `VERIFIED` | P1 | doc / API | `doc/OpenAPI.yaml` 落后于真实路由，缺少 favorites、thumb、file、favorite 等接口。 | 前后端联调和外部客户端会按过期契约实现。 | 已从 `service/src/api/mod.rs` 对齐 OpenAPI，并标注 thumb/file 二进制响应接口。 |
| XP-20260425-05 | `VERIFIED` | P1 | web / album | 相册列表为统计照片数对每个相册并发调用 `/photos/search`。 | 相册数量增多后产生 N+1 请求风暴，拖慢前端并增加 SQLite 压力。 | 已改为后端 `/albums` 返回 `photo_count`，前端相册列表一次请求完成。 |
| XP-20260425-06 | `VERIFIED` | P2 | repo / docs | `AGENTS.md` 仍写着仓库为空、未发现源文件。 | 后续协作和自动化代理会拿到错误仓库状态。 | 已更新为当前 Rust service、静态 web、doc 和实际验证命令。 |
| XP-20260425-07 | `VERIFIED` | P1 | service / formatting | `cargo fmt --all -- --check` 当前失败，有大量 rustfmt diff。 | 提交前检查不通过，后续代码 diff 噪声会扩大。 | 已执行全后端 rustfmt，格式检查通过。 |
| XP-20260425-08 | `VERIFIED` | P0 | service / tests | `cargo test` 当前失败，失败用例为 `tests/error_recovery.rs::cancel_scan_should_reach_cancelled`。 | 项目当前不能宣称后端测试集通过。 | 已随 XP-20260425-01 修复，完整 `cargo test` 通过。 |
| XP-20260425-09 | `VERIFIED` | P0 | service / testdata | `testdata_builder` 生成的 EXIF `DateTimeOriginal` 未被扫描逻辑识别为 `shot_at`，样例入库后 `shot_at` 为 `NULL`。 | 后续测试若依赖生成数据，会漏测拍摄时间、排序时间和日期筛选能力。 | 已修复 EXIF 时间解析，并增加基于 `testdata_builder sample` 的 shot_at/GPS/album 集成测试。 |
| XP-20260425-10 | `VERIFIED` | P1 | service / scripts | `scripts/smoke_test.sh` 的 fs_watch `changed_paths` 硬编码为 `/tmp/xphoto_test_data`，未使用 `DATA_DIR`。 | 使用自定义数据目录时 smoke 不能真实覆盖当前目录路径。 | 已改为使用 `DATA_DIR` 生成 `fs_watch.changed_paths`，并统一用 JSON 序列化构造请求体。 |
| XP-20260425-11 | `VERIFIED` | P1 | service / tests | 当前集成测试仍在各文件内手写测试图片和 helper，没有复用 `testdata_builder` 或统一 fixtures。 | 后续测试基线分散，无法稳定覆盖真实目录规则、EXIF、GPS、搜索和批量数据场景。 | 已抽出 `service/tests/common`，并让样例数据集测试依赖 `testdata_builder sample` manifest。 |
| XP-20260425-12 | `VERIFIED` | P2 | service / testdata | `family_us_weekends_2025` 批量生成速度约 7.5 张/秒，2025 年 2567 张预计约 5 分半；CLI 也缺少 `--clean`、manifest 和严格参数校验。 | 不适合作为常规 CI 默认数据集，重复运行也不容易做确定性断言。 | 已增加 `sample`、小/中/完整策略、manifest 输出、`--clean` 和未知参数报错。 |
| XP-20260425-13 | `VERIFIED` | P1 | service / lint | `cargo clippy --all-targets -- -D warnings` 当前失败，包含 `collapsible_if`、`too_many_arguments`、`large_enum_variant`、`type_complexity`、`items_after_test_module` 等问题。 | 后端提交前 lint 门禁无法通过，也会掩盖后续真实新增 warning。 | 已完成 clippy 清理，lint 门禁通过。 |
| XP-20260425-14 | `VERIFIED` | P0 | service / scan | 全量重扫只 upsert 当前存在的文件，没有把磁盘上已删除的照片同步为 `deleted_at`。 | 本地相册删除文件后，搜索、收藏、相册和封面仍可能展示不存在的照片。 | 已在非 retry 扫描中同步软删除缺失照片，清理收藏/搜索索引并修正相册封面。 |
| XP-20260425-15 | `VERIFIED` | P1 | service / tests | 后端缺少删除同步、二进制照片接口、收藏分页、相册手工维护的集成测试。 | 这些核心用户流程容易在后续改动中回归，尤其是跨表状态一致性。 | 已新增 `tests/backend_coverage.rs` 覆盖四组核心流程。 |
| XP-20260425-16 | `VERIFIED` | P1 | service / testdata | 缺少可持续复用的大数据测试集生成入口，只能手动拼 `testdata_builder` 命令。 | 大规模扫描稳定性测试难以复现，也容易重复生成浪费时间。 | 已新增 `service/scripts/build_large_testdata.sh`，支持 profile、manifest summary、跳过已存在场景和显式重建。 |
| XP-20260425-17 | `VERIFIED` | P1 | service / testdata | 大数据测试集生成仍是单线程逐张写入，`large/stress` profile 生成时间过长。 | 开始扫描压力测试前，准备数据本身耗时过高，影响迭代效率。 | 已为 `testdata_builder` 增加 rayon 并行生成和 `--jobs` 参数，脚本透传并记录 jobs。 |
| XP-20260425-18 | `VERIFIED` | P0 | service / stress | 缺少可复现的扫描压力测试 harness 和正确性报告。 | 大数据并发扫描卡死或出错时，无法判断是性能瓶颈、数据不一致还是具体阶段失败。 | 已新增 `scan_stress_test.sh`、scan summary 日志锚点和 JSON/Markdown 报告。 |
| XP-20260425-19 | `VERIFIED` | P1 | doc / config | `doc/config.example.toml` 的 `source_change_detect_enabled` 仍为 `true`，与代码默认 `false` 不一致。 | 按示例配置启动压测时可能误开启目录变更检测，干扰扫描吞吐和结果。 | 已同步为 `false` 和 30000ms。 |
| XP-20260425-20 | `VERIFIED` | P0 | service / stress | 压测只覆盖纯扫描吞吐，缺少删除、追加、取消、失败重试等大数据正确性压力场景。 | 并发扫描即使吞吐正常，也可能在跨轮状态收敛、取消和 retry 下出现计数不一致或状态错误。 | 已新增 mutation stress 脚本并完成 `family-full-2021` 场景验证。 |

## 修复计划/处理记录

### 2026-04-25 初始记录

- 建立本文档，作为后续问题跟踪入口。
- 初始问题来自当前 doc/service/web review 和一次后端验证。
- 当前验证结论：
  - `cargo fmt --all -- --check` 失败。
  - `cargo test` 失败，失败用例是 `cancel_scan_should_reach_cancelled`，实际状态为 `success`，期望为 `cancelled`。

### 2026-04-25 XP-20260425-01 修复记录

- 修复范围：
  - running scan 收到取消请求时立即将 `scan_jobs.status` 切换为 `cancelled`，并写入 `source_scan_states`。
  - task cancel 联动 scan cancel 时使用同一套 running scan 取消路径。
  - scan 成功收尾改为条件写入：只有 `status='running'` 且 `cancel_requested=0` 时才能切换为 `success`。
  - 若 success 提交前发现取消请求，改写为 `cancelled`，避免尾部竞态覆盖取消结果。
- 验证结果：
  - `cargo test --test error_recovery cancel_scan_should_reach_cancelled -- --nocapture` 通过。
  - `cargo test` 通过。
  - `cargo fmt --all -- --check` 仍失败，归属 `XP-20260425-07`，本次未扩大处理。

### 2026-04-25 测试数据工具 review 记录

- 已验证：
  - `cargo run --bin testdata_builder -- /tmp/xphoto_test_data_sample_review --strategy sample --year 2025 --seed 42` 可生成样例 JPEG。
  - 使用临时后端和 SQLite 执行 `BASE_URL=http://127.0.0.1:58081/rpc/v1 DATA_DIR=/tmp/xphoto_test_data_sample_review TIMEOUT_SEC=60 ./scripts/smoke_test.sh` 通过。
  - 样例数据可扫描入库，可自动生成 `Sample.Album`，GPS 可写入 `gps_lat/gps_lng`。
- 新发现：
  - 样例照片入库后 `shot_at` 为 `NULL`，但 `exif_json` 中能看到 `DateTimeOriginal`，说明生成器和扫描解析逻辑没有完全对齐。
  - `family_us_weekends_2025` 在 2025 年 seed 42 下计划生成 2567 张，试跑速度约 7.5 张/秒，常规自动化测试不应默认跑完整大集。
  - smoke 脚本 fs_watch 路径未使用 `DATA_DIR`。

### 2026-04-25 XP-20260425-09 修复记录

- 修复范围：
  - 扫描 EXIF 解析改为遍历所有字段提取时间，不再只按 `In::PRIMARY` 查询。
  - 时间提取优先级明确为 `DateTimeOriginal > DateTime`。
  - `parse_exif_datetime_to_rfc3339` 兼容 EXIF 库显示出的 `YYYY-MM-DD HH:MM:SS` 和原始 `YYYY:MM:DD HH:MM:SS`。
  - 新增基于 `CARGO_BIN_EXE_testdata_builder --strategy sample` 的集成测试，断言 `shot_at`、`sort_time`、GPS 和自动相册。
- 验证结果：
  - `cargo test --test scan_workflow generated_sample_data_should_scan_exif_and_album -- --nocapture` 通过。
  - `cargo test` 通过。
  - `git diff --check` 通过。
  - `cargo fmt --all -- --check` 仍失败，归属 `XP-20260425-07`，本次未混入全仓格式化。

### 2026-04-25 XP-20260425-02 / XP-20260425-03 修复记录

- XP-20260425-02 修复范围：
  - 普通手动扫描和 fs_watch 扫描不再读取 `source_scan_states.last_scanned_path` 作为恢复游标。
  - `resume_enabled` 仅对 `retry` 触发的扫描生效，避免新一轮扫描跳过历史路径。
  - 成功扫描写入 `source_scan_states` 时清空 source 级 `last_scanned_*` 游标字段；取消/失败路径仍保留游标供 retry 使用。
  - 新增手动重扫回归测试：先扫 `z_existing.jpg`，再新增排序更靠前的 `a_new_before_cursor.jpg`，第二次手动扫描必须入库新文件。
- XP-20260425-03 修复范围：
  - 默认配置和自动生成的 `config.toml` 都将 `scan.source_change_detect_enabled` 改为 `false`，变更检测变成显式 opt-in。
  - 显式开启变更检测时，source 递归遍历通过 `tokio::task::spawn_blocking` 执行，避免阻塞 async runtime。
  - 变更检测 fingerprint 完成后记录 `entry_count` 和耗时，便于后续判断大目录成本。
- 验证结果：
  - `cargo test --test scan_workflow manual_rescan_should_not_resume_after_previous_success -- --nocapture` 通过。
  - `cargo test config::tests -- --nocapture` 通过。
  - `cargo test` 通过。
  - `git diff --check` 通过。
  - `cargo fmt --all -- --check` 仍失败，归属 `XP-20260425-07`。
  - `cargo clippy --all-targets -- -D warnings` 失败，归属新登记的 `XP-20260425-13`，本次未混入全局 lint 清理。

### 2026-04-25 XP-20260425-07 / XP-20260425-13 修复记录

- XP-20260425-07 修复范围：
  - 执行 `cargo fmt --all`，统一后端 Rust 源码格式。
- XP-20260425-13 修复范围：
  - 清理 clippy 提示的可折叠分支、连续 `replace`、`matches!`、`is_multiple_of` 等机械 lint。
  - 将 `run_scan_job` 参数收敛为 `ScanRunOptions`，降低函数参数数量。
  - 将批量扫描结果中的大体积候选对象装箱，降低 enum variant 尺寸。
  - 为 EXIF 扫描返回值增加 `ExifScanData` 类型别名，降低类型复杂度。
  - 派生 `StorageConfig::Default`，并整理 `local_fs` 测试模块位置。
  - 提取配置环境变量解析 helper，减少重复嵌套分支。
- 验证结果：
  - `cargo fmt --all -- --check` 通过。
  - `cargo clippy --all-targets -- -D warnings` 通过。
  - `cargo test` 通过。

### 2026-04-25 XP-20260425-04 / XP-20260425-06 修复记录

- XP-20260425-04 修复范围：
  - `doc/OpenAPI.yaml` 补齐当前真实路由中的 `/photos/favorites`、`/photos/{photo_id}/thumb`、`/photos/{photo_id}/file`、`/photos/{photo_id}/favorite`。
  - 为缩略图和文件接口标注二进制响应 content type 与缓存头。
  - 补充 `UpdatePhotoFavoriteRequest`、收藏分页响应、`Photo`、`FavoritePhotoItem` 等 schema。
  - 补齐 `CreateAlbumRequest.cover_photo_id` 字段。
- XP-20260425-06 修复范围：
  - 重写 `AGENTS.md` 的当前仓库状态，不再声称仓库为空。
  - 补充 Rust backend、静态 web、OpenAPI、测试数据构建、smoke test 和验证命令。
  - 明确 OpenAPI 源头、问题跟踪文档和前端无 Node toolchain 的现状。
- 验证结果：
  - 已对照 `service/src/api/mod.rs` 路由列表检查 OpenAPI 缺失项。
  - `python3` + `PyYAML` 解析 `doc/OpenAPI.yaml` 通过，并确认新增 path 存在。
  - `git diff --check` 通过。
  - `cargo fmt --all -- --check` 通过。
  - `cargo clippy --all-targets -- -D warnings` 通过。

### 2026-04-25 XP-20260425-05 修复记录

- 修复范围：
  - 后端 `/albums` 返回项新增 `photo_count`，通过 `LEFT JOIN photo_albums/photos` 一次性统计未删除照片数。
  - 新增 `AlbumListItem` API 响应类型，避免修改核心 `Album` domain model。
  - 前端相册卡片直接使用 `photo_count`，移除相册列表加载后的逐相册 `/photos/search` 统计逻辑。
  - `doc/OpenAPI.yaml` 同步 `/albums` 返回 schema，补充 `AlbumListItem.photo_count`。
  - 集成测试增加 `/albums` 的 `photo_count` 断言。
- 验证结果：
  - `cargo test --test scan_workflow -- --nocapture` 通过。
  - 使用临时后端 `127.0.0.1:58082` 和静态 web `127.0.0.1:58083` 做 browser-use 验证：刷新相册页仅请求 `/rpc/v1/albums`，不再请求 `/rpc/v1/photos/search` 统计相册数量。

### 2026-04-25 XP-20260425-10 / XP-20260425-11 / XP-20260425-12 修复记录

- XP-20260425-10 修复范围：
  - `scripts/smoke_test.sh` 创建 source 和触发 fs_watch 的请求体改为由 Python `json.dumps` 构造。
  - fs_watch `changed_paths` 改为使用当前 `DATA_DIR`，不再硬编码 `/tmp/xphoto_test_data`。
- XP-20260425-11 修复范围：
  - 新增 `service/tests/common`，统一测试 app 构造、JSON API 调用、source 创建、scan 触发/等待、测试图片生成和 sample 数据生成。
  - `scan_workflow`、`error_recovery`、`task_control` 复用公共 helper，减少重复测试基础设施代码。
  - `generated_sample_data_should_scan_exif_and_album` 读取 `testdata_builder sample` 生成的 manifest，作为后续 fixture 基线。
- XP-20260425-12 修复范围：
  - `testdata_builder` CLI 增加严格参数解析，未知参数和缺失参数值会报错退出。
  - 增加 `--clean` 和 `--manifest <path>`。
  - 增加 `family_us_weekends_small`、`family_us_weekends_medium`，保留 `family_us_weekends_2025` 完整策略。
  - manifest 记录策略、年份、seed、输出目录、相册计数和每张照片的路径/EXIF/GPS/mtime。
  - `doc/测试数据构建工具.md` 更新 CLI、策略和 manifest 说明。
- 验证结果：
  - `cargo run --bin testdata_builder -- /tmp/xphoto_builder_sample --strategy sample --year 2025 --seed 42 --clean --manifest /tmp/xphoto_builder_sample/manifest.json` 通过。
  - `cargo run --bin testdata_builder -- /tmp/xphoto_builder_small --strategy family_us_weekends_small --year 2025 --seed 42 --clean --manifest /tmp/xphoto_builder_small/manifest.json` 通过。
  - 未知参数负向验证通过：`--unknown` 会失败并打印 usage。
  - 使用包含空格的 `DATA_DIR="/tmp/xphoto smoke data"` 运行 `scripts/smoke_test.sh` 通过。
  - `cargo test --test scan_workflow -- --nocapture` 通过。
  - `cargo test --test error_recovery -- --nocapture` 通过。
  - `cargo test --test task_control -- --nocapture` 通过。
  - `cargo fmt --all -- --check` 通过。
  - `cargo clippy --all-targets -- -D warnings` 通过。
  - `cargo test` 通过。

### 2026-04-25 XP-20260425-14 / XP-20260425-15 修复记录

- XP-20260425-14 修复范围：
  - 扫描预加载现有照片时记录 `photo_id`，处理当前扫描结果时记录已见到的 `storage_file_id`。
  - 非 retry 扫描完成写入后，对预加载但本轮未见到的照片执行软删除。
  - 删除同步会清理 `photo_favorites`、`photo_search_fts`，并对受影响相册执行封面有效性修正。
  - 删除同步数量计入 `updated_count`，便于任务状态反映本轮有状态变更。
- XP-20260425-15 修复范围：
  - 新增 `service/tests/backend_coverage.rs`。
  - 覆盖删除同步：重扫后缺失文件不再出现在搜索/详情/收藏中，相册计数和封面会修正。
  - 覆盖二进制接口：`/photos/{id}/file` 和 `/photos/{id}/thumb` 返回 JPEG bytes 与正确 content type。
  - 覆盖收藏分页：验证 `total`、分页边界和按 `favorite_at` 倒序。
  - 覆盖相册手工维护：创建、添加、改名、更新备注、搜索索引刷新、设置/移除封面和空相册封面清空。
  - `service/tests/common` 增加 raw HTTP 调用 helper，用于二进制响应断言。
  - `album_add_photos` 补充 album 存在校验，并避免把已删除或不存在的照片插入相册关联。
- 验证结果：
  - `cargo test --test backend_coverage -- --nocapture` 通过。
  - `cargo fmt --all -- --check` 通过。
  - `cargo clippy --all-targets -- -D warnings` 通过。
  - `cargo test` 通过。

### 2026-04-25 XP-20260425-16 修复记录

- 修复范围：
  - 新增 `service/scripts/build_large_testdata.sh`。
  - 支持 `sample`、`medium`、`large`、`stress` profile。
  - 默认不清理数据，已有 manifest 和场景目录时跳过，便于大数据集长期复用。
  - 支持 `--force` 按场景重建，支持 `--clean` 删除整个输出目录后重建。
  - 自动生成 `_manifests/_summary_<profile>.json`，汇总场景、照片数、相册数和 manifest 路径。
  - `doc/测试数据构建工具.md` 增加大数据集生成和使用说明。
- 验证结果：
  - `bash -n service/scripts/build_large_testdata.sh` 通过。
  - `service/scripts/build_large_testdata.sh --help` 通过。
  - `service/scripts/build_large_testdata.sh --output /tmp/xphoto_large_script_sample --profile sample --clean` 通过。
  - `git diff --check` 通过。

### 2026-04-25 XP-20260425-17 修复记录

- 修复范围：
  - `testdata_builder` 增加 `--jobs <n>`，`0` 表示自动按 CPU 核心数选择。
  - 使用 `rayon` 在线程池内并行生成图片、写 EXIF、设置 mtime。
  - manifest 仍按原始计划顺序收集，保持输出可复现和便于 diff。
  - 进度条改为线程安全计数，继续显示总进度、速率和 ETA。
  - `build_large_testdata.sh` 增加 `--jobs`，并透传给每个场景；summary 记录 jobs。
  - `doc/测试数据构建工具.md` 更新并行生成说明。
- 验证结果：
  - `cargo fmt --all -- --check` 通过。
  - `cargo clippy --all-targets -- -D warnings` 通过。
  - `cargo run --bin testdata_builder -- /tmp/xphoto_parallel_sample --strategy sample --jobs 2 --clean --manifest /tmp/xphoto_parallel_sample/manifest.json` 通过。
  - `service/scripts/build_large_testdata.sh --output /tmp/xphoto_large_script_parallel --profile sample --jobs 2 --clean` 通过。
  - `git diff --check` 通过。

### 2026-04-25 XP-20260425-18 / XP-20260425-19 修复记录

- XP-20260425-18 修复范围：
  - 新增 `service/scripts/scan_stress_test.sh`。
  - 默认启动隔离 service、临时 SQLite 和临时日志目录，避免污染当前开发库。
  - 支持 `single` 模式把整个数据集作为一个 source 扫描。
  - 支持 `scenarios` 模式按 manifest 场景目录创建多个 source，以覆盖并发扫描。
  - 报告输出到 `doc/reports/scan_stress_<label>.json|md`。
  - 正确性检查包括 job 状态、失败数、API 搜索总数、DB active photo 数、FTS 行数和重复 storage 分组。
  - `run_scan_job` 增加完成阶段 summary 日志，记录 collect、hash/exif、DB upsert、FTS、相册链接、删除同步、封面修正和总体吞吐。
- XP-20260425-19 修复范围：
  - `doc/config.example.toml` 中 `source_change_detect_enabled` 改为 `false`。
  - `source_change_detect_interval_ms` 同步为当前默认的 `30000`。
- 验证结果：
  - `bash -n service/scripts/scan_stress_test.sh` 通过。
  - `service/scripts/scan_stress_test.sh --help` 通过。
  - `SCAN_MAX_CONCURRENT_JOBS=2 SCAN_HASH_PARALLELISM=4 service/scripts/scan_stress_test.sh --data-root /home/bucky/.xphoto/test_data --profile stress --mode scenarios --max-scenarios 2 --label pilot-scenarios-2 --timeout-sec 300` 通过。
  - 试跑报告：`doc/reports/scan_stress_pilot-scenarios-2.md`。
  - 使用 `/home/bucky/.xphoto/test_data` 跑完整第一轮 `single`/`scenarios` 压测矩阵通过，覆盖扫描并发 2/4、hash 并行度 0/4/8、batch 32/64/128。
  - 矩阵汇总报告：`doc/reports/scan_stress_matrix_20260425.md`。
  - 6 个 case 均满足 API 搜索总数、DB active photo 数、FTS 行数等于 `17227`，失败 job 数为 `0`，重复 storage 分组为 `0`。
  - 并发 8 重复矩阵 `stress-scenarios-cj8-hp4-bs64-r1/r2/r3` 通过，3 次耗时为 15s、15s、16s。
  - `cargo fmt --all -- --check` 通过。
  - `cargo clippy --all-targets -- -D warnings` 通过。
  - `git diff --check` 通过。

### 2026-04-25 XP-20260425-20 修复记录

- 修复范围：
  - 新增 `service/scripts/scan_mutation_stress_test.py`。
  - 脚本启动隔离 service、临时 SQLite 和临时日志目录。
  - 删除/追加场景使用临时复制的数据目录，避免修改 `/home/bucky/.xphoto/test_data`。
  - 覆盖初始扫描、删除后重扫、追加后重扫、大数据扫描中取消、不存在源路径失败后 retry。
  - 报告输出到 `doc/reports/scan_mutation_stress_<label>.json|md`。
- 验证结果：
  - `python3 -m py_compile service/scripts/scan_mutation_stress_test.py` 通过。
  - `SCAN_MAX_CONCURRENT_JOBS=4 SCAN_HASH_PARALLELISM=4 SCAN_HASH_BATCH_SIZE=64 service/scripts/scan_mutation_stress_test.py --data-root /home/bucky/.xphoto/test_data --profile stress --scenario family-full-2021 --label mutation-stress-full2021 --bind-addr 127.0.0.1:58130 --timeout-sec 3600 --delete-count 25 --append-count 10 --cancel-delay-sec 0.2` 通过。
  - 初始扫描 `3018` 张，删除 `25` 张后 active/API/FTS 收敛到 `2993`，追加 `10` 张后 active/API/FTS 收敛到 `3003`。
  - 大数据扫描中取消终态为 `cancelled`。
  - 不存在源路径首次扫描为 `failed`，创建文件后 retry 为 `success`，active/API/FTS 均为 `1`。
  - 报告：`doc/reports/scan_mutation_stress_mutation-stress-full2021.md`。

## 下一步建议

1. 增加矩阵重复执行汇总脚本，自动计算 min/mean/max 吞吐和失败摘要。
2. 增加真正的扫描中 mutation 场景：扫描运行期间追加/删除文件，并验证下一轮全量重扫可以收敛。
3. 继续补齐后端边界覆盖：异常源路径、批量删除后的相册清理、fs_watch changed_paths 精准化、OpenAPI 与新增行为同步检查。
